---
trigger: always_on
---

# Kill and Restart on Run

When I type "run", do this:

1. **Kill** any existing app instance
2. **Restart** the app

Always do both steps. Never skip step 1.

when run in terminal aware below setup
在這個版本中 '&&' 語彙基元不是有效的陳述式分隔符號